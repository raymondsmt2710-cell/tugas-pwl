<nav class="sticky top-0 z-50 bg-white shadow-md">
    <div class="max-w-7xl mx-auto px-8 py-5 flex justify-between items-center">

        <a href="{{ route('home') }}" class="text-3xl font-bold text-green-600">
            Autopahala
        </a>

        <div class="space-x-6 font-semibold">
            <a href="{{ route('home') }}" class="hover:text-green-600">
                Home
            </a>

            <a href="{{ route('about') }}" class="hover:text-green-600">
                About
            </a>

            <a href="{{ route('faq') }}" class="hover:text-green-600">
                FAQ
            </a>

            <a href="{{ route('contact') }}" class="hover:text-green-600">
                Contact
            </a>
        </div>

    </div>
</nav>